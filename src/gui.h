#ifndef GUI_H
#define GUI_H

#include <gtk/gtk.h>

/**
 * Creates and shows the main calculator application window.
 * @param app The GtkApplication instance.
 */
void create_main_window(GtkApplication *app);

#endif /* GUI_H */
